# Local implementation guides (not for public companion repo)
docs/SEARCH_PROTOCOL_AND_REPO_BUILD.md
docs/PROTOCOL_V1.1_AMENDMENT.md
docs/PROTOCOL_V1.2_AMENDMENT.md
docs/PHASE2_CLOSURE_CORRECTIONS.md

# Derived screening batches — regenerable from candidate-pool.csv + screening-log.csv
# via screening/scripts/make_batches.py. The decisions themselves (screening/decisions/)
# ARE tracked: they are the audit trail and must not be gitignored.
screening/batches/

# Secrets — never commit API keys
local.env
.env
.env.*
!.env.example
